# MongoDB
Username=""
Password=""
DB_URI=""                       # Atlas connection string, with the database name in it

# Redis
REDIS_URL=""

# VoyageAI
VOYAGE_API_KEY=""
Embedding_Model=""              # voyage-4-large (locked — the professions were embedded with it)

# Anthropic
ANTHROPIC_API_KEY=""

# Razorpay
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
RAZORPAY_WEBHOOK_SECRET=""      # set when Razorpay goes live; subscribe payment.captured, refund.processed, refund.failed
PAYMENT_MODE="manual"           # manual | razorpay

# Resend
RESEND_API_KEY=""
EMAIL_FROM=""

# Google OAuth (redirect flow — redirect URI: http://localhost:5000/auth/google/callback)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL=""          # http://localhost:5000/auth/google/callback in dev

# Auth
JWT_SECRET=""                   # replaces AUTH_KEY from CODING_STYLE.md. Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Server
PORT="5000"
FRONTEND_URL=""                 # http://localhost:3000 in dev (Create React App)

# First admin — used once by `npm run seed:admin`
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
ADMIN_NAME=""
