node_modules/
.env
.env.local
/Backend/.env
/Frontend/.env
/Frontend/build/
.DS_Store

# Raw Baseline_Rating sample passes. Disposable by design — every sample they contain is preserved
# inside Backend/data/baseline_rating.json under sample_variance, so the merged file is the record.
/.ratings/
